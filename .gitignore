# Dependencies
node_modules/

# Production build
dist/

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
logs/
*.log

# Backtest CSV output (regenerated on demand)
backtest-results.csv

# IDEs and editors
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.DS_Store

# NOTE: data/ledger.json is intentionally NOT ignored.
# The ledger is our zero-infra "database" and is committed to the repo so
# GitHub Actions can persist pick statuses across stateless daily runs.
